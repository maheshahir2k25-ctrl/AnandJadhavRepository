<UserControl x:Class="DependencyProperties.UCAlertBanner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DependencyProperties"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" x:Name="AlertControl">
    

    <!--<Border Style="{StaticResource MyUserControlStyle}">
        <TextBlock Style="{StaticResource MyUserControlTextStyle}" Text="{Binding AlertMessage}"/>
    </Border>-->
    <Border BorderBrush="Red" BorderThickness="1" Background="#FFDDDD" 
            Padding="10" CornerRadius="4" Margin="5">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="{Binding ElementName=Root, Path=AlertMessage}" 
                       Foreground="Red" FontWeight="SemiBold" 
                       VerticalAlignment="Center"/>

            <Button Grid.Column="1" Content="X" 
                    Command="{Binding ElementName=Root, Path=CloseCommand}"
                    Width="25" Height="25" Margin="10,0,0,0"
                    Background="White" BorderBrush="Red"/>
        </Grid>
    </Border>
    
</UserControl>
