<Window x:Class="DataBinding.Collections_ItemsControls"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DataBinding"
        mc:Ignorable="d"
        Title="Collections_ItemsControls" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="InputBox" Width="200" Margin="0,0,10,0"/>
            <Button Content="Add" Click="AddButton_Click" Width="60"/>
        </StackPanel>

        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding TodoItems, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                 FontSize="16"/>

        <Button Grid.Row="2" 
                Content="Remove Top Item" 
                Click="DeleteButton_Click" 
                Margin="0,10,0,0"/>
    </Grid>
</Window>
